services:
  redeemer:
    build: .
    container_name: m5rcels-redeemer
    restart: unless-stopped
    ports:
      - "10513:10513"
    environment:
      NODE_ENV: production
      PORT: 10513
      ADMIN_SECRET: change-this-secret
      DB_PATH: /data/redeemer.sqlite
    volumes:
      - redeemer-data:/data

volumes:
  redeemer-data:
